/**
 * 📱 Native localStorage polyfill
 * Installs expo-sqlite backed localStorage on native platforms.
 * On web, localStorage exists natively (see install-storage.ts).
 */
import "expo-sqlite/localStorage/install";
